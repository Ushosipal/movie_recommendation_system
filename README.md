# movie_recommendation_system
A content-based movie recommendation web app built using Flask, Python, and HTML/CSS. It suggests similar movies based on user selection using cosine similarity applied to vectorized movie metadata using CountVectorizer.
